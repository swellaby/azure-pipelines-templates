parameters:
  invokeOptions: ''
  tasksAndArguments: ''
  taskDisplayName: 'Run Invoke'

steps:
- script: 'invoke ${{ parameters.invokeOptions }} ${{ parameters.tasksAndArguments }}'
  displayName: '${{ parameters.taskDisplayName }}'
